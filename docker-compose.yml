services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: aircraft_records
      POSTGRES_USER: aircraft
      POSTGRES_PASSWORD: aircraft
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
